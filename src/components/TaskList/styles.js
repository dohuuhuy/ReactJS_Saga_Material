const styles = (params) => (
    {
        box: {
            display: "flex",
            alignItems: "center"
        },
        shape: {
            padding: 20,
            margin: 10,
            backgroundColor: "red"
        }
    });


export default styles;