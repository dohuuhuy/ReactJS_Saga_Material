const styles = (params) => (
    {
        CardActions: {
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'flex-end',
        }

    });


export default styles;